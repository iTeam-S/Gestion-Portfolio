import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.edit.angular',
  appName: 'edit-portfolio',
  webDir: 'dist/edit-portfolio',
  bundledWebRuntime: false
};

export default config;
